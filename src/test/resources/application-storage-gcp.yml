spring:
  config:
    activate:
      on-profile: storage-gcp

  cloud:
    gcp:
      storage:
        credentials:
          location: src/test/resources/storage/gcs-key.json
    project-id: airy-charmer-406518
    bucket: devdevdev-storage